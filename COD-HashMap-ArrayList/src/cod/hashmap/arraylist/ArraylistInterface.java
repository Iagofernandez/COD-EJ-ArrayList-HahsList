
package cod.hashmap.arraylist;

//creamos una nueva interfaz
public interface ArraylistInterface {
    //creamos un metodo void que no delvuelve nada, solo sirve para mostrar
     public void putElement(String element);
     //creamos un metodo que devuelve un string mediante un int
     public String pushElement(int index);
}
