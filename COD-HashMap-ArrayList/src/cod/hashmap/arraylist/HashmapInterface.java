
package cod.hashmap.arraylist;

// creamos una interfaz nueva
public interface HashmapInterface {
    //creamos un metodo abstracto vacia que nos muestra una variable element String.
    public void getValue(String element);
    // creamos un metodo abstracto que devuelve un string.
    public String pushValue(int key);
}
